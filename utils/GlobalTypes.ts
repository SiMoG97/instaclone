export type SignupStepOneTypes = {
  phoneEmail: string;
  fullName: string;
  userName: string;
  password: string;
};
export type SignupStepTwoTypes = {
  day: number;
  month: number;
  year: number;
};

export type LoginFormTypes = {
  userNamephoneEmail: string;
  password: string;
};
